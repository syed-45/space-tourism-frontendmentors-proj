import { Activity, } from "react";
import spaceData from "@/starter-code/data.json"

export const AllCrewImages = ({destinationName}: {destinationName: string}) => {

    return (
        <>
            
        </>
        
    )
}